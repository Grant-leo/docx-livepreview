{
  "name": "docx-livepreview",
  "displayName": "DOCX Live Preview",
  "description": "WPS-native DOCX preview for academic papers, with OMML math, CJK fonts, source sync, and WPS-rendered layout.",
  "version": "0.2.7",
  "publisher": "docx-chat",
  "icon": "media/icon.png",
  "repository": "https://github.com/Grant-leo/docx-livepreview",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "docx",
    "WPS",
    "preview",
    "academic",
    "OMML",
    "math",
    "CJK"
  ],
  "main": "./out/extension.js",
  "activationEvents": [
    "onCustomEditor:docx.docxPreview",
    "onCommand:docx.goToPreview",
    "onCommand:docx.goToSource",
    "onCommand:docx.openPreview",
    "onCommand:docx.refreshPreview",
    "onStartupFinished"
  ],
  "contributes": {
    "customEditors": [
      {
        "viewType": "docx.docxPreview",
        "displayName": "WPS DOCX Preview",
        "selector": [
          {
            "filenamePattern": "*.docx"
          }
        ],
        "priority": "default"
      }
    ],
    "configurationDefaults": {
      "workbench.editorAssociations": {
        "*.docx": "docx.docxPreview"
      }
    },
    "commands": [
      {
        "command": "docx.goToPreview",
        "title": "Go to Preview",
        "category": "DOCX"
      },
      {
        "command": "docx.goToSource",
        "title": "Go to Source",
        "category": "DOCX"
      },
      {
        "command": "docx.openPreview",
        "title": "Open with WPS Preview",
        "category": "DOCX"
      },
      {
        "command": "docx.refreshPreview",
        "title": "Refresh Preview",
        "category": "DOCX"
      }
    ],
    "configuration": {
      "title": "DOCX Live Preview",
      "properties": {
        "docx.pythonPath": {
          "type": "string",
          "default": "python",
          "description": "Path to Python 3.8+ executable"
        },
        "docx.autoRefresh": {
          "type": "boolean",
          "default": true,
          "description": "Automatically refresh preview when the docx file changes"
        },
        "docx.defaultZoom": {
          "type": "number",
          "default": 100,
          "minimum": 25,
          "maximum": 500,
          "description": "Default zoom percentage"
        },
        "docx.renderDpi": {
          "type": "number",
          "default": 200,
          "minimum": 72,
          "maximum": 600,
          "description": "Render DPI (higher = sharper, slower, more memory)"
        },
        "docx.sourceScript": {
          "type": "string",
          "default": "",
          "description": "Path to the Python build script. Supports ${workspaceFolder}."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "tsc -p ./",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "^25.7.0",
    "@types/vscode": "1.85.0",
    "typescript": "^5.3.0"
  }
}
